<Window x:Class="CookMaster.Views.ForgotPasswordView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CookMaster.Views"
        mc:Ignorable="d"
        Title="ForgotPasswordView" Height="300" Width="400">
    <Border Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                
                <RowDefinition Height="Auto"/>
                
                <RowDefinition Height="12"/>
                <RowDefinition Height="Auto"/>
                
                <RowDefinition Height="Auto"/>
                
                <RowDefinition Height="12"/>
                <RowDefinition Height="Auto"/>
                
                <RowDefinition Height="Auto"/>
                
                <RowDefinition Height="18"/>
                <RowDefinition Height="Auto"/>
                
            </Grid.RowDefinitions>

            <!-- Username -->
            <TextBlock Grid.Row="0" Text="Put in your username to get your security question:" Margin="0,0,0,4"/>
            <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBox   Grid.Row="1" HorizontalAlignment="Center" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                   Width="220" Height="28"/>
                <Button Content="Get Question" Command="{Binding GetQuestionCommand}" Margin="8,0,0,0" Width="100" Height="28"/>
            </StackPanel>
            
            <TextBlock Grid.Row="3" Text="Security question answer:" Margin="0,0,0,4"/>
            <StackPanel Grid.Row="4" Orientation="Horizontal">
                <TextBox   Grid.Row="4" Text="{Binding SecurityAnswer, UpdateSourceTrigger=PropertyChanged}"
                           Width="220" Height="28"/>
                <Button Content="Submit Answer" Command="{Binding SubmitAnswerCommand}" Margin="8,0,0,0" Width="100" Height="28"/>
            </StackPanel>
           

            
            <TextBlock Grid.Row="6" Text="New Password" Margin="0,0,0,4"/>
            <StackPanel Grid.Row="7" Orientation="Horizontal">
                <TextBox  Grid.Row="7" Width="220" Height="28" Text="{Binding NewPassword, UpdateSourceTrigger=PropertyChanged}"
                          IsEnabled="{Binding AnswerVerified}"/>
                
                
            </StackPanel>
            <TextBlock Grid.Row="8" Text="Confirm New Password" Margin="0,0,0,5"/>

            <StackPanel Grid.Row="9" Orientation="Horizontal">
                <TextBox  Grid.Row="9" Width="220" Height="28" Text="{Binding ConfirmNewPassword, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,8,0"/>
                <Button  Grid.Row="9" Content="Change password" Width="100" Height="28"
                Command="{Binding ChangePasswordCommand}"
                         IsEnabled="{Binding CanChangePassword}"
                 HorizontalAlignment="Right" Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
