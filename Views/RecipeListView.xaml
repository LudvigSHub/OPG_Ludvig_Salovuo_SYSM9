<Window x:Class="CookMaster.Views.RecipeListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CookMaster.Views"
        mc:Ignorable="d"
        Title="CookMaster" Height="300" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
            
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*" />
                <ColumnDefinition Width="5* "/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Recipe Manager" 
                       HorizontalAlignment="Center" VerticalAlignment="Center" 
                       Margin="0,0,0,0" 
                       FontSize="24" FontWeight="Bold" />
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="0">
            
            <Button Grid.Column="1" Content="Add" Width="75" Height="30" Margin="0,0,5,0" Command="{Binding AddCommand}"/>
            <Button Grid.Column="1" Content="Delete" Width="75" Height="30" Margin="0,0,5,0" Command="{Binding DeleteCommand}"/>
           
        </StackPanel>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="7*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!--ItemsSource sätter binging direkt till objektet Items i MainWindowViewModel -->
            <!--SelectedItem binder markerat item med prop SelectedItem i MainWindowViewModel -->
            <DataGrid CanUserAddRows="False" 
                  CanUserDeleteRows="False" 
                   
                  AutoGenerateColumns="False">

                <!--Hanterar Bindings till properties i item -->
                <!--Skriver sedan ut datan i rätt datacolumn, de skapade itemsen då från MainWindowViewModel-->
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="4*"/>
                    <DataGridTextColumn Header="Serial Number" Binding="{Binding SerialNumber}" Width="4*"/>
                    <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="2*"/>
                </DataGrid.Columns>

                <!--Binding item SelectedItem.(Property) för att välja specifik property av selected item till varje textbox  -->
            </DataGrid>
            <StackPanel Height="auto" Grid.Column="1">
                <Button Name="AddRecipe" Content="Add Recipe" Height="30" Margin="5,5,5,0"/>
            </StackPanel>


        </Grid>

    </Grid>
</Window>
